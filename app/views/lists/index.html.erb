<h1>Lists</h1>

<div id="lists">
  <% @lists.each do |list| %>
    <%= render list %>
    <p>
      <%= link_to "Show this category", list %>
    </p>
  <% end %>
</div>

<%= link_to "Add Category", new_list_path %>
