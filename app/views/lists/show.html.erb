<h1><%= @list.name %></h1>

<div id="teams">
  <% @list.movies.each do |movie| %>
    <%= movie.title %>
    <%= movie.overview %>
    <%= movie.poster_url %>
    <br>
    <%= movie.rating %>
  <% end %>
</div>

<%= link_to "Boookmark", new_list_bookmark_path(@list) %>
